# Supabase (프로젝트: AIMaster / esgxyikcnnvmlhygjkth — 다른 서브프로젝트와 동일 프로젝트 공유)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# 사이트 기본 URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_MAIN_SITE_URL=https://buylife.xyz

# AI 키/카카오(SOLAPI) 연동은 전부 회원 본인이 /settings에서 등록하는 BYOK 방식이라
# 이 앱 자체의 환경변수로는 등록하지 않습니다 (user_api_keys / user_solapi_accounts 참고).

# 카카오 로그인("나에게 보내기" 무료 API) — REST API 키/Client Secret은 2026-09-16부터
# 회원 본인이 /settings에서 등록하는 BYOK 방식으로 바뀌어(user_api_keys의
# kakao_rest_api_key/kakao_client_secret) 더 이상 이 앱의 환경변수로 두지 않는다(카카오 앱이
# "비즈니스 앱 전환" 심사를 받지 않은 동안은 테스터로 등록된 계정만 로그인 가능해, 앱 공용
# 키 하나로는 운영자 본인 외 다른 회원이 연동할 수 없었기 때문 — threads의 THREADS_APP_ID
# 전환과 동일한 이유). Redirect URI만은 이 앱의 고정 배포 도메인 기준이라 여전히 환경변수로
# 둔다 — 회원이 본인 카카오 앱을 만들 때도 이 값을 그대로 Redirect URI에 등록해야 한다.
KAKAO_REDIRECT_URI=http://localhost:3000/api/kakao/callback

# 카카오톡 "공유하기"(회원이 리포트를 원하는 채팅방에 직접 공유) — 위 KAKAO_REST_API_KEY와
# 같은 카카오 앱의 "JavaScript 키"를 그대로 쓰면 된다(새 앱을 만들 필요 없음). 다만 이 사이트
# 도메인을 카카오 개발자 콘솔의 "플랫폼 키 > JavaScript SDK 도메인"과 "제품 링크 관리 > 웹
# 도메인" 두 곳 모두에 등록해야 공유 카드 클릭이 정상 동작한다(mbti-character에서 실제로
# 겪은 트러블슈팅 — 둘 중 하나만 등록하면 카드는 보여도 클릭 시 이동이 안 될 수 있다).
NEXT_PUBLIC_KAKAO_JS_KEY=

# 예약 작업(cron) 보호용 — Phase 3(정기 자동 발송)에서 사용
CRON_SECRET=
